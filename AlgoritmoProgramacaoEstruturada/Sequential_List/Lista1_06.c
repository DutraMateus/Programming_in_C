//Faça o mesmo exercício, atribuindo valor às variávies A e B (não é necessário solicitar ao usuário, os valores serão atribuídos no próprio código)//

#include<stdio.h>
    int main(){
    int A=2;
    int B=3;
    int X;

    X= A + B;

    printf("\n%d + %d = %d", A, B, X);

}
